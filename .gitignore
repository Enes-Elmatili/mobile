# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# env files (NEVER commit secrets)
.env
.env.*
.env*.local

# Clés de service Google (SECRET — jamais committées)
#   google-service-account.json  = clé Play Console, utilisée par `eas submit` (eas.json)
#   firebase-adminsdk-*.json     = clé FCM V1, à uploader dans `eas credentials` puis supprimer
google-service-account.json
*firebase-adminsdk*.json
# NB : google-services.json n'est PAS ignoré — EAS en a besoin au moment du prebuild,
# et il est de toute façon embarqué en clair dans l'APK (ce n'est pas un secret).

# typescript
*.tsbuildinfo

app-example

# generated native folders
/ios
/android

# Backup junk
*.bak

